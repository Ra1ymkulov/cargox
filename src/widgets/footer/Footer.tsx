"use client";
import React from "react";
import scss from "./Footer.module.scss";

interface IFooterProps {}

const Footer = ({}) => {
  return <footer className={scss.footer}>Footer</footer>;
};

export default Footer;
