import React from "react";
import scss from "./Button.module.scss";

interface IButtonProps {}

const Button = ({}) => {
  return <button className={scss.button}></button>;
};

export default Button;
