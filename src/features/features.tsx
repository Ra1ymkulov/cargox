import React from "react";

const features = () => {
  return (
    <div>
      <p>
        Функционал проекта - логин, добавление в избранное, фильтрация списка
      </p>
    </div>
  );
};

export default features;
